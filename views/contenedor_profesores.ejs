<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Profesores</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    
    <style>
        /* Tema morado institucional */
        nav { background: linear-gradient( #04144a 100%); }
        .modal h4 { color: #667eea; }
        
        /* Ajuste para Tabla */
        .table-container {
            max-height: 60vh;
            overflow: auto;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            border-radius: 8px;
        }
        thead th {
            position: sticky;
            top: 0;
            z-index: 10;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
    </style>
</head>
<body class="grey lighten-4">

    <nav>
        <div class="nav-wrapper container">
            <a href="#" class="brand-logo left hide-on-small-only" style="font-size: 1.5rem;">
                <i class="material-icons left">assignment_ind</i>Gestión de Profesores
            </a>
            <ul id="nav-mobile" class="right">

                <li><a href="/contenedor_admin"><i class="material-icons left">people</i>Menu</a></li>
                
                <li><a href="/alumnos"><i class="material-icons left">people</i>Alumnos</a></li>
                <li><a href="/cursos"><i class="material-icons left">class</i>Cursos</a></li>
                <li><a href="/notas"><i class="material-icons left">assessment</i>Notas</a></li>
                <li><a href="/logout" class="red darken-3 waves-effect waves-light btn"><i class="material-icons left">exit_to_app</i>Salir</a></li>
            </ul>
        </div>
    </nav>

    <div class="container" style="margin-top: 20px; width: 95%; max-width: 1200px;">
        
        <div class="card-panel">
            <h5 class="center-align" style="color: #495057; font-weight: 600;">➕ Registrar Nuevo Profesor</h5>
            <form action="/crear_profesor" method="POST" class="row" style="margin-bottom: 0;">
                
                <div class="input-field col s12 m3">
                    <i class="material-icons prefix">badge</i>
                    <input id="rut" name="rut" type="text" class="validate" required placeholder="12.345.678-9">
                    <label for="rut">RUT</label>
                </div>

                <div class="input-field col s12 m3">
                    <i class="material-icons prefix">person</i>
                    <input id="nombre" name="nombre" type="text" class="validate" required>
                    <label for="nombre">Nombres</label>
                </div>

                <div class="input-field col s12 m3">
                    <i class="material-icons prefix">person_outline</i>
                    <input id="apellido" name="apellido" type="text" class="validate" required>
                    <label for="apellido">Apellidos</label>
                </div>

                <div class="input-field col s12 m3 center-align">
                    <button class="btn waves-effect waves-light green darken-1" type="submit" style="width: 100%; height: 45px;">
                        Guardar
                        <i class="material-icons right">add_circle</i>
                    </button>
                </div>
            </form>
        </div>

        <div class="row mb-0">
            <div class="input-field col s12 m6 offset-m3">
                <i class="material-icons prefix">search</i>
                <input id="buscador_profesores" type="text" class="validate">
                <label for="buscador_profesores">🔍 Buscar por Nombre, Apellido o RUT</label>
            </div>
        </div>

        <div class="card table-container">
            <table class="highlight centered striped">
                <thead>
                    <tr>
                        <th>RUT</th>
                        <th>Nombres</th>
                        <th>Apellidos</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (profesores && profesores.length > 0) { %>
                        <% profesores.forEach(p => { %>
                        <tr>
                            <td style="font-weight: bold;"><%= p.rut || 'Sin RUT' %></td>
                            <td><%= p.nombre %></td>
                            <td><%= p.apellido %></td>
                            <td>
                                <a href="#modalEdicionProfesor" 
                                   class="btn-small amber darken-2 modal-trigger waves-effect waves-light" 
                                   onclick="cargarProfesor('<%= p.id_docente %>', '<%= p.rut %>', '<%= p.nombre %>', '<%= p.apellido %>')">
                                   <i class="material-icons">edit</i>
                                </a>
                                <a href="/borrar_profesor/<%= p.id_docente %>" 
                                   class="btn-small red darken-2 waves-effect waves-light" 
                                   onclick="return confirm('¿Seguro que deseas desactivar al profesor <%= p.nombre %> <%= p.apellido %>?');">
                                   <i class="material-icons">delete</i>
                                </a>
                            </td>
                        </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="4" class="grey-text center-align" style="padding: 20px;">
                                No hay profesores registrados en el sistema.
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>

    <div id="modalEdicionProfesor" class="modal" style="max-width: 500px; border-radius: 8px;">
        <form action="/editar_profesor" method="POST">
            <div class="modal-content">
                <h4>✏️ Editar Profesor</h4>
                <div class="row" style="margin-top: 20px;">
                    <input type="hidden" name="id_docente" id="id_profesor_editar">

                    <div class="input-field col s12">
                        <i class="material-icons prefix">badge</i>
                        <input id="rut_editar" name="rut" type="text" class="validate" required>
                        <label for="rut_editar">RUT</label>
                    </div>

                    <div class="input-field col s12 m6">
                        <i class="material-icons prefix">person</i>
                        <input id="nombre_editar" name="nombre" type="text" class="validate" required>
                        <label for="nombre_editar">Nombres</label>
                    </div>

                    <div class="input-field col s12 m6">
                        <input id="apellido_editar" name="apellido" type="text" class="validate" required>
                        <label for="apellido_editar">Apellidos</label>
                    </div>
                </div>
            </div>
            
            <div class="modal-footer">
                <a href="#!" class="modal-close waves-effect waves-red btn-flat">Cancelar</a>
                <button type="submit" class="waves-effect waves-green btn blue darken-1">Guardar Cambios</button>
            </div>
        </form>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            M.AutoInit();
        });

        // Función para cargar los datos en el Modal
        function cargarProfesor(id, rut, nombre, apellido) {
            document.getElementById('id_profesor_editar').value = id;
            document.getElementById('rut_editar').value = rut;
            document.getElementById('nombre_editar').value = nombre;
            document.getElementById('apellido_editar').value = apellido;

            M.updateTextFields();
        }

        // Script del Buscador en tiempo real
        const inputBusqueda = document.getElementById('buscador_profesores');
        if (inputBusqueda) {
            inputBusqueda.addEventListener('keyup', function() {
                const texto = this.value.toLowerCase();
                const filas = document.querySelectorAll('table tbody tr');
                
                filas.forEach(fila => {
                    if(fila.cells.length === 1) return; // Ignorar fila de "No hay profesores"

                    const rut = fila.cells[0].textContent.toLowerCase();
                    const nombre = fila.cells[1].textContent.toLowerCase();
                    const apellido = fila.cells[2].textContent.toLowerCase();
                    
                    if (rut.includes(texto) || nombre.includes(texto) || apellido.includes(texto)) {
                        fila.style.display = ''; 
                    } else {
                        fila.style.display = 'none'; 
                    }
                });
            });
        }
    </script>
</body>
</html>